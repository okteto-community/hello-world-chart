# we add the repository as a dependency to have it build first
dependencies:
  hello-world-hola-mundo:
    repository: https://github.com/okteto-community/hello-world-chart
    branch: ${HELLO_WORLD_BRANCH:-main}
    manifest: okteto.yaml
    # wait: true
    variables:
      DEPLOY: "false"

  mychart:
    repository: https://github.com/okteto-community/hello-world-chart
    branch: "jjc/parallel-test"
    manifest: okteto-template.yml
    # wait: true
    variables:
      CHART_DIRECTORY: "mychart"
      APP_NAME: "mychart-name"
deploy:
  - name: Placeholder
    command: |
      echo "This is a placeholder for the deploy step. You can add your deployment commands here."
